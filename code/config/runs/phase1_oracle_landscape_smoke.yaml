# python scripts/phase1_oracle_landscapes.py --config config/runs/phase1_oracle_landscape_smoke.yaml
kind: phase1_oracle_landscape
run_tag: phase1_smoke

fleet_config: config/fleet.yaml
controller_config: config/controller.yaml
fleet_overrides:
  clients_per_regime: 10   # 3 regimes x 10 = 30 clients

n_sobol: 64                # power of 2, per scipy.stats.qmc.Sobol's balance recommendation
n_calibration_episodes: 3
n_test_episodes: 3
episode_base_seed: 5000
identification_seed_offset: 1000

n_clusters: 3
cluster_seed: 1337

fig_a_grid_resolution: 15  # per-axis resolution for the Figure-A 2D landscape slice

seed: 42
n_jobs: -1
