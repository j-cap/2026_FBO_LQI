# python scripts/phase1_oracle_landscapes.py --config config/runs/phase1a_matched_model_full_v2.yaml
#
# Full-scale Phase 1A rerun (v2) after fixing the state_scale/beta_max_deg
# regime-blindness bug (see phase1a_matched_model_smoke_v2.yaml and
# docs/phase1_diagnostic_findings.md). Confirms the fix at doc's recommended scale
# (60 clients, M=256) and re-checks whether the transfer-loss finding survives with
# the outlier-donor confound removed.
kind: phase1_oracle_landscape
run_tag: phase1a_matched_model_full_v2

fleet_config: config/fleet.yaml
controller_config: config/controller.yaml
fleet_overrides:
  clients_per_regime: 20

n_sobol: 256
n_calibration_episodes: 10
n_test_episodes: 50
episode_base_seed: 5000
identification_seed_offset: 1000
n_identification_episodes: 8

n_clusters: 3
cluster_seed: 1337

fig_a_grid_resolution: 25

seed: 42
n_jobs: -1
