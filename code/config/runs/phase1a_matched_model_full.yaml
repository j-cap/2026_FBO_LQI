# python scripts/phase1_oracle_landscapes.py --config config/runs/phase1a_matched_model_full.yaml
#
# Full-scale Phase 1A ("matched-model case", correction-plan item 4), confirming
# results/phase1a_matched_model_smoke at doc's recommended scale (60 clients, M=256)
# before drawing conclusions. Same fixes as the smoke run (state normalization,
# held-out calibration/test separation, physical constraints) - only clients_per_regime
# and n_sobol change.
kind: phase1_oracle_landscape
run_tag: phase1a_matched_model_full

fleet_config: config/fleet.yaml
controller_config: config/controller.yaml
fleet_overrides:
  clients_per_regime: 20

n_sobol: 256
n_calibration_episodes: 10
n_test_episodes: 50
episode_base_seed: 5000
identification_seed_offset: 1000
n_identification_episodes: 8

n_clusters: 3
cluster_seed: 1337

fig_a_grid_resolution: 25

seed: 42
n_jobs: -1
