# python scripts/phase1_oracle_landscapes.py --config config/runs/phase1_oracle_landscape_full.yaml
# Run only after the smoke config's output has been sanity-checked (see plan's "Run order").
kind: phase1_oracle_landscape
run_tag: phase1_full

fleet_config: config/fleet.yaml
controller_config: config/controller.yaml
fleet_overrides:
  clients_per_regime: 20   # 3 regimes x 20 = 60 clients, doc §5.2's recommended default

n_sobol: 256                # doc §10.1's recommended M for a 3-D tuning space
n_calibration_episodes: 3
n_test_episodes: 3
episode_base_seed: 5000
identification_seed_offset: 1000

n_clusters: 3
cluster_seed: 1337

fig_a_grid_resolution: 25

seed: 42
n_jobs: -1
